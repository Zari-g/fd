# Notebooks

Exploratory notebooks may be added here in future iterations. Reusable loading,
validation, analysis, and modeling logic should live in `src/fraud_detection`
rather than only in notebooks.

